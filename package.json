{
  "name": "styled-scopify",
  "version": "0.0.2",
  "author": "@Losant",
  "license": "MIT",
  "main": "lib/index.js",
  "files": "lib/**/*.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Losant/styled-scopify.git"
  },
  "scripts": {
    "build": "babel src --out-dir lib --no-comments",
    "prepublishOnly": "yarn build",
    "postversion": "git push && git push --tags"
  },
  "dependencies": {
    "ramda": "^0.25.0",
    "styled-transform-proxy": "^0.0.1"
  },
  "devDependencies": {
    "@losant/eslint-config-losant": "^1.2.6",
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.0",
    "babel-plugin-transform-imports": "^1.4.1",
    "babel-preset-env": "^1.6.1"
  }
}
